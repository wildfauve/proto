$(function() {
/*
	var memurl, name;
	memurl = $("a").attr("href");
	name = $(".member").attr("id");
	localStorage.setItem("name", name);
*/
	$('.register').click(function() {
		alert('Handler for .click() called.');
	});
});